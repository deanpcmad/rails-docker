
FROM ruby:3.4.5-alpine AS base

LABEL maintainer="dean@deanpcmad.com"

RUN apk add --no-cache \
    build-base \
    gcompat \
    postgresql-dev \
    sqlite-dev \
    mariadb-dev \
    git \
    bash \
    tzdata \
    yaml-dev \
    nodejs-current \
    npm \
    yarn
